. ${HOME}/opt/rtools/bin/detect-os > /dev/null

case ${os_version} in
    *)
        case $(hostname) in
            *)
                ;;
        esac
        ;;
esac

# Rust
if [ -d "$HOME/.cargo" ]; then
    . "$HOME/.cargo/env"
fi

export _LOADED_ZSHRC_=1
