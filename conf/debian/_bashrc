. ${HOME}/opt/rtools/bin/detect-os > /dev/null

case $(hostname) in
    host1)
        case ${os_version} in
            *)
                ;;
        esac
        ;;
    *)
        ;;
esac

# Rust
if [ -d "$HOME/.cargo" ]; then
    . "$HOME/.cargo/env"
fi

export _BASHRC_LOADED_=1
