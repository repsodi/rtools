. ${HOME}/opt/rtools/bin/env.conf > /dev/null

case ${os_conf} in
    *)
        ;;
esac

case $(hostname) in
    *)
        ;;
esac

if [ "${shell_name}" = "zsh" ]; then
    # Oh-My-Zsh
    unset PAGER
    unset LESS

    export _LOADED_RC_ZSH=1
fi

# Rust
if [ -d "$HOME/.cargo" ]; then
    . "$HOME/.cargo/env"
fi

export _LOADED_RC=1
