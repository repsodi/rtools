#!/bin/bash

if [ "${1}" = "" ] || [ "${2}" = "" ] || [ "${3}" = "" ]; then
    echo "3 args are required."
    exit
fi

. $(dirname $0)/../../conf/ubuntu/_profile
. $(dirname $0)/../../conf/ubuntu/_bashrc

set-profile
set-bashrc
set-gitconfig ${1} ${2}
set-smb ${3}
