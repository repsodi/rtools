#!/bin/bash

if [ "${1}" = "" ] || [ "${2}" = "" ] || [ "${3}" = "" ]; then
    echo "3 args are required."
    exit
fi

. $(dirname $0)/../../conf/fedora/_bash_profile
. $(dirname $0)/../../conf/fedora/_bashrc

set-bash-profile
set-bashrc
set-gitconfig ${1} ${2}
set-smb ${3}
