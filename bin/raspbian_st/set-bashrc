#!/bin/sh

export OS_VER=raspbian_st
export FILE=$HOME/.bashrc
export CONFIG=$HOME/opt/rtools/conf/$OS_VER/_bashrc

if [ ! -f $FILE.ori ]; then
    echo "Creating $FILE.ori..."
    cp --preserve=timestamps $FILE $FILE.ori
else
    echo "$FILE.ori exists."
fi

cat $FILE.ori $CONFIG > $FILE
echo "$FILE has been generated."
ls -la $FILE*
